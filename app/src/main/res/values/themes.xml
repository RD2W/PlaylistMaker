<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlaylistMaker" parent="Theme.MaterialComponents.DayNight.NoActionBar"> <!--Theme.Material3.DayNight.NoActionBar-->
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_blue</item>
        <item name="android:statusBarColor">@color/white</item> <!-- Светлый фон системного статус бара -->
        <item name="android:windowLightStatusBar">true</item> <!-- Тёмные иконки в системном статус баре-->
        <item name="cursorColor">@color/light_blue</item>
    </style>

    <style name="Theme.PlaylistMaker" parent="Base.Theme.PlaylistMaker">
        <item name="snackbarStyle">@style/CustomSnackbarStyle</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.PlaylistMaker.MaterialAlertDialog</item>
    </style>
</resources>